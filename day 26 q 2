x#include <stdio.h>

int main() {
    int groups[] = {4, 5, 3, 1}; 
    int n = sizeof(groups) / sizeof(groups[0]);

    for (int i = 0; i < n; i++) {
        for (int j = 0; j < groups[i]; j++) {
            printf("*\n");
        }
        printf("\n"); 
    }

    return 0;
}
